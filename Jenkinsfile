pipeline{
    agent any

    environment {
        AWS_DEFAULT_REGION = 'eu-west-1'
        S3_BUCKET = 'ratings-component'
    }

    stages {
        stage('Checkout'){
            echo 'hello ratings component'
        }
    }
}